import React from "react";
import { View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "victory-native";

export const AddNewCalendarTutor = () => {
  return (
    <SafeAreaView>
      <Text>Calendar tutor</Text>
    </SafeAreaView>
  );
};
